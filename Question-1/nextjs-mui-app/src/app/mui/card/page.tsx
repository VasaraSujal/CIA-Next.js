import { Card, CardContent, Typography } from '@mui/material';

const CardPage = () => {
    return (
        <Card>
            <CardContent>
                <Typography variant="h5">Route: /mui/card</Typography>
                <Typography>This is the Card component page.</Typography>
            </CardContent>
        </Card>
    );
};

export default CardPage;