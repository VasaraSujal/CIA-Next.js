import Typography from '@mui/material/Typography';

const TypographyPage = () => {
    return (
        <div>
            <Typography variant="h4">Route: /mui/typography</Typography>
        </div>
    );
};

export default TypographyPage;